---
title: "ABC472"
pubDate: 2026-08-24
description: "AtCoder Beginner Contest 472のA〜C問題を解いた記録"
author: "kurosiko"
image:
  url: "https://docs.astro.build/assets/rose.webp"
  alt: "ピンク色に輝く暗い背景に浮かぶAstroのロゴ。"
tags: ["AtCoder", "競技プログラミング", "C++"]
---

# ABC472

## A問題

**結果:** AC

別に悪い回答ではない。ほぼ解説と同じ。

```cpp
#include <iostream>
#include <string>

using namespace std;

int main() {
    string s;
    cin >> s;

    for (char c : s) {
        cout << (c != 'A' ? '.' : 'A');
    }

    return 0;
}
```

なんかforの部分嫌だなとか思ってたらこういう書き方もできるらしい。
もはや可読性はどこ

```cpp
for (char c : s) cout << ".A"[c == 'A'];
```

## B問題

**結果:** AC

O(n^2)は避けれてよかった。
累積和を偶然にも思いついたので

```cpp
#include <algorithm>
#include <cstdlib>
#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n;
    cin >> n;

    vector<int> l(n);
    int sum = 0;

    for (auto& i : l) {
        cin >> i;
        sum += i;
        i = sum;
    }

    int ans = sum;

    for (int i = 0; i < n - 1; ++i) {
        ans = min(abs(sum - l[i] * 2), ans);
    }

    cout << ans << endl;
    return 0;
}
```

## C問題

**結果:** 未AC

解説を見て、とりあえず差分計算を使うことを知ってあとは自力実装
したかったが、llだけ最後まで気づかず...
制約は確認しようね

```cpp
#include <algorithm>
#include <iostream>
#include <vector>

using namespace std;

#define rep(i, n) for (int i = 0; i < (n); ++i)

using ll = long long;

int main() {
    ll n, m, k;
    cin >> n >> m >> k;

    vector<ll> a(n);
    for (auto& i : a) cin >> i;

    ll partial_sum = 0;

    rep(i, n) {
        partial_sum += a[i];
        if (i >= m) {
            partial_sum -= a[i - m];
        }

        if (partial_sum > k) {
            partial_sum -= a[i];
            a[i] = 0;
            cout << "No\n";
        } else {
            cout << "Yes\n";
        }
    }

    return 0;
}
```

差分計算は、以下のような場面で使えるらしい。

- スライディングウィンドウ（しゃくとり法）
- いもす法（Imos Algorithm） / 階差配列
- 動的集合の管理（`std::multiset` や転倒数）
- 木の上での差分計算（Tree Imos / 全方位木DP）

いずれか解きたいね...
