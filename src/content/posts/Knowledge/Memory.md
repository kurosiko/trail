---
title: "Memory"
pubDate: 2026-09-01
description: "ASMをやるついでに調べたメモリ関係"
author: "kurosiko"
image:
  url: "https://docs.astro.build/assets/rose.webp"
  alt: "ピンク色に輝く暗い背景に浮かぶAstroのロゴ。"
tags: ["picoCTF"]
---

# Memory

まぁ、アセンブリの問題を解いているときに、色々考えて調べたのでメモ。

## メモリ階層について

| 速度・価格 | 記憶装置 | アクセス時間の目安 | 容量の目安 |
| --- | --- | --- | --- |
| ▲（速・高） | レジスタ（Register） | CPUが命令から直接扱う | ISAから見えるものは数十本程度 |
| │ | L1キャッシュ（SRAM） | 約 1 ns（数サイクル） | 32〜128 KB（コアごと） |
| │ | L2キャッシュ（SRAM） | 約 3〜4 ns（数〜十数サイクル） | 512 KB〜数 MB（コアごと） |
| │ | L3キャッシュ（SRAM） | 約 10〜20 ns（数十サイクル） | 数 MB〜数百 MB（共有） |
| │ | 主記憶（メインメモリ、DRAM） | 約 50〜100 ns | 8 GB〜64 GB以上 |
| ▼（遅・安） | 二次記憶（NVMe SSD / HDD） | 10 µs〜10 ms | 256 GB〜数 TB |
| | アーカイブ記憶（テープなど） | 秒〜分 | 数 TB〜PB |

数値はCPUの世代、メモリの種類、アクセスパターンなどで大きく変わる、ざっくりした目安です。
クロック周波数が3.2 GHzなら1周期は約0.31 nsで、周期は周波数の逆数になります。なお、DDRメモリの「DDR4-3200」などの転送速度を、CPUのクロック周波数3,200 MHzと同じものとして扱うことはできません。
ただし、周波数が高ければすべてのメモリアクセスや演算が同じ割合で速くなる、という意味ではありません。

アセンブリ言語の命令では、レジスタやメモリに加えて、命令に直接埋め込まれた即値も扱います。CPUの命令からはキャッシュとRAMを通常は区別せず、同じメモリ空間としてアクセスします。

このページでは主にx86-64のレジスタと呼び出し規約を扱います。ARM64については
[ARM64版のメモ](/posts/knowledge/memory-arm64)を参照してください。

## x86-64のレジスタ

x86-64では、たとえば次の汎用レジスタと命令ポインタがあります。

`rax`, `rbx`, `rcx`, `rdx`, `rsi`, `rdi`, `rbp`, `rsp`,
`r8`〜`r15`, `rip`

```text
64-bit 全体                  [ RAX ]
32-bit（下位32ビット）       [ EAX ]
16-bit（下位16ビット）       [ AX  ]
 8-bit（AXの上位8ビット）    [ AH  ]
 8-bit（AXの下位8ビット）    [ AL  ]
```

このように使い分けます。`EAX`、`AX`、`AH`、`AL`は、それぞれ`RAX`の一部を指す名前です。`EAX`などへの32ビット書き込みでは、x86-64の仕様上、`RAX`の上位32ビットが0になります。

### 主な役割・慣習

| レジスタ | 主な役割・慣習 |
| --- | --- |
| `rax` | 整数・ポインタの戻り値（返り値）に使う（ABIによる） |
| `rsp` | スタックポインタ。スタックの先頭を指すため、通常は自由に使わない |
| `rbp` | フレームポインタに使うことがある。省略時は汎用レジスタとして使える |
| `rip` | 次に実行する命令の位置を指す命令ポインタ |

引数をどのレジスタに渡すかは、x86-64のチップではなくOS・ABI（呼び出し規約）で決まります。

| 規約 | 整数・ポインタ引数のレジスタ（左から第1引数） |
| --- | --- |
| System V AMD64 ABI（Linux、macOSなど） | `rdi`, `rsi`, `rdx`, `rcx`, `r8`, `r9` |
| Microsoft x64（Windows） | `rcx`, `rdx`, `r8`, `r9` |

表の個数を超えた引数は、規約に従ってスタックなどに渡されます。Windows x64では、呼び出し側が4つの引数レジスタ用のシャドウ領域も用意します。
浮動小数点引数は、System V AMD64では主に`xmm0`〜`xmm7`、Microsoft x64では主に`xmm0`〜`xmm3`も使うなど、別の分類があります。構造体の渡し方や戻り値の扱いにも別の規則があります。

### 参考資料

- [x86-64 psABI](https://gitlab.com/x86-psABIs/x86-64-ABI)
- [Microsoft x64 calling convention](https://learn.microsoft.com/en-us/cpp/build/x64-calling-convention)
