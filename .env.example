# Required for production. Generate with: openssl rand -hex 32
VISITOR_COUNTER_SECRET=replace-with-a-long-random-secret

# Optional. Defaults to .data/visitors.json for the standalone Node server.
# VISITOR_COUNTER_FILE=.data/visitors.json

# Set only when your reverse proxy overwrites forwarding headers.
# VISITOR_TRUST_PROXY=true
